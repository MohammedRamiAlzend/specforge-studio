# Session Results

This directory contains the user-facing Markdown result for each completed implementation session.

Each file uses the format `YYYY-MM-DD-short-session-name.md` and records the session status, completed result, verification evidence, changed artifacts, and follow-up work. These files supplement rather than replace the continuity records in `memory/`.
